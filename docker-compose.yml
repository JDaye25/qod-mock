services:
  qod-api:
    build: .
    container_name: qod-api
    ports:
      - "8010:8000"
    env_file:
      - .env
    environment:
      - QOD_ARTIFACTS_DIR=/app/artifacts
    volumes:
      - ./artifacts:/app/artifacts
    restart: unless-stopped