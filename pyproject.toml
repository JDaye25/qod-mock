[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "qod-mock"
version = "0.1.0"
description = "QoD mock service"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["backend", "src", "tests"]